import React from 'react';

const ThoughtSection = ({ projects, onProjectSelect }) => {
  return (
    <div style={{ padding: '100px 40px', backgroundColor: 'white', minHeight: 'calc(100vh - 50px)' }}>
      <h2 style={{ fontWeight: '300', textTransform: 'uppercase', letterSpacing: '0.1em' }}>Thought Map</h2>
      <p style={{ color: '#666', marginTop: '10px' }}>The relational flowchart view is under construction.</p>
    </div>
  );
};

export default ThoughtSection;